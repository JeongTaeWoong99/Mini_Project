# 🔄 Liskov Substitution Principle (리스코프 치환 원칙)

## 📋 목차
- [풀어서 설명](#-풀어서-설명)
- [원칙 개요](#-원칙-개요)
- [왜 LSP가 필요한가?](#-왜-lsp가-필요한가)
- [핵심 개념](#-핵심-개념)
- [코드 구조](#-코드-구조)
- [Before & After 비교](#-before--after-비교)
- [주요 코드 분석](#-주요-코드-분석)
- [장단점](#-장단점)
- [실제 적용 사례](#-실제-적용-사례)
- [학습 정리](#-학습-정리)

---

## 💡 풀어서 설명

### 한 문장으로 이해하기

```
부모 타입으로 선언된 변수에 자식 객체를 넣어도,
아무 문제 없이 예상대로 작동해야 한다.
```

### 실생활 비유

**❌ 나쁜 예 : 기차는 교통수단을 대체 못함**
```
"교통수단"으로 일반 도로를 이동하라고 했을 때...

- 자동차 사용? → ✅ 문제없이 잘 감!
- 기차 사용?   → ❌ 선로가 없어서 못 감!

문제점 :
→ 기차는 "교통수단"을 대체할 수 없음!
→ 기차는 선로라는 특수한 조건이 필요함!
```

**✅ 좋은 예 : 자동차는 교통수단을 대체 가능**
```
"교통수단"으로 일반 도로를 이동하라고 했을 때...

- 세단 사용? → ✅ 문제없이 잘 감!
- SUV 사용?  → ✅ 문제없이 잘 감!
- 트럭 사용? → ✅ 문제없이 잘 감!

장점 :
→ 모든 자동차가 "교통수단"을 완벽하게 대체!
→ 부모 타입으로 사용해도 일관되게 작동!
```

### 쉽게 이해하기

**"대체 가능하다"의 의미** :
- 부모 타입 변수에 자식 객체를 넣어도 문제없다
- 타입 체크 (`if (object is ChildType)`)가 필요 없다
- 모든 자식이 일관되게 동작한다

### 판단 기준

**"타입 체크가 필요한가?"**

- ✅ **타입 체크 불필요** → LSP 준수! 잘 설계됨!
- ❌ **타입 체크 필요** → LSP 위반! 설계 수정 필요!

```csharp
// ❌ LSP 위반 징후
if (powerUp is SpeedBoost)  // 타입 체크 필요!
{
    // 특별 처리...
}

// ✅ LSP 준수
powerUp.ApplyEffect(player);  // 타입 체크 없이 일관되게 작동!
```

---

## 🎯 원칙 개요

**Liskov Substitution Principle (LSP)** 은 **SOLID 원칙**의 세 번째 원칙으로, 하위 클래스는 상위 클래스를 완벽하게 대체할 수 있어야 한다는 원칙입니다.

### 📌 핵심 개념

```
부모 타입 변수에 자식 객체를 넣어도, 예상대로 작동해야 한다!
```

**잘못된 설계 :**
```csharp
// ❌ 기본 클래스에 duration 개념 없음
public abstract class UnrefactoredPowerUp
{
    public abstract void ApplyEffect(GameObject player);
}

// ❌ 하위 클래스가 임의로 duration 추가
public class UnrefactoredSpeedBoost : UnrefactoredPowerUp
{
    public float m_Duration = 5f;  // 기본 클래스에 없는 개념!

    public override void ApplyEffect(GameObject player)
    {
        if (m_Duration > 0) { ... }  // 조건부 동작
        else { ??? }  // 혼란스러운 동작
    }
}
// 문제 : duration 때문에 다른 PowerUp과 일관성이 깨짐!
//        대체 불가능!
```

**LSP 적용 :**
```csharp
// ✅ 기본 클래스에 duration 개념 포함
public abstract class PowerUp : MonoBehaviour
{
    [SerializeField] protected float m_Duration;  // ⭐ 공통 개념!
    public abstract void ApplyEffect(GameObject player);
}

// ✅ 하위 클래스들이 일관되게 동작
public class HealthBoost : PowerUp { }        // duration 미사용 (즉시)
public class SpeedBoost : PowerUp { }         // duration 사용 (지속)
public class InvulnerabilityPowerUp : PowerUp { }  // duration 사용 (지속)

// 모든 PowerUp이 서로 대체 가능! ✅
```

---

## 🤔 왜 LSP가 필요한가?

### 문제 상황

게임에서 파워업 시스템을 만들 때, 기본 클래스를 잘못 설계한 경우 :

```csharp
// 기본 클래스 : duration 개념 없음
public abstract class UnrefactoredPowerUp : MonoBehaviour
{
    public abstract void ApplyEffect(GameObject player);
}

// 속도 증가 파워업 : duration 필요!
public class UnrefactoredSpeedBoost : UnrefactoredPowerUp
{
    public float m_Duration = 5f;  // 하위 클래스에서 추가

    public override void ApplyEffect(GameObject player)
    {
        if (m_Duration > 0)  // 조건부 동작
        {
            speedModifier.ModifySpeed(2f, m_Duration);
        }
        else
        {
            // duration 없으면? 어떻게 하지?
        }
    }
}
```

**이 코드의 문제점 :**

❌ **대체 불가능**
   - 부모 타입(UnrefactoredPowerUp)으로 선언된 변수에 자식 객체 사용 시 일관성 깨짐
   - duration 있는 것과 없는 것이 섞임

❌ **확장성 부족**
   - 새로운 파워업 추가 시 duration 처리 방식 혼란
   - 각 하위 클래스가 독자적인 방식으로 구현

❌ **유지보수 어려움**
   - 기본 클래스의 계약(contract)이 불명확
   - 어떤 파워업이 어떻게 동작할지 예측 불가

❌ **테스트 어려움**
   - 모든 PowerUp을 동일하게 테스트할 수 없음
   - 각각 다른 방식으로 테스트 필요

### LSP의 해결책

✅ **일관된 인터페이스**
   - 모든 하위 클래스가 같은 계약 준수

✅ **완벽한 대체 가능성**
   - 부모 타입으로 사용해도 문제없음

✅ **명확한 설계**
   - 기본 클래스에 공통 개념 정의

✅ **쉬운 확장**
   - 새로운 파워업 추가 시 규칙 명확

---

## 🏗️ 핵심 개념

LSP를 이해하기 위한 핵심 질문 :

### ❓ "부모 타입 변수에 자식 객체를 넣어도 예상대로 작동하는가?"

**나쁜 설계 (대체 불가능) :**
```csharp
// 부모 타입으로 선언
UnrefactoredPowerUp powerUp;

// 자식 객체 대입
powerUp = new UnrefactoredSpeedBoost();  // duration 필수
powerUp.ApplyEffect(player);

powerUp = new SomeOtherPowerUp();        // duration 개념 없음
powerUp.ApplyEffect(player);

// ❌ 각각 다르게 동작! 일관성 없음!
```

**좋은 설계 (대체 가능) :**
```csharp
// 부모 타입으로 선언
PowerUp powerUp;

// 자식 객체들 대입
powerUp = new HealthBoost();
powerUp.ApplyEffect(player);  // ✅ m_Duration 미사용 (즉시 회복)

powerUp = new SpeedBoost();
powerUp.ApplyEffect(player);  // ✅ m_Duration 사용 (일시적 증가)

powerUp = new InvulnerabilityPowerUp();
powerUp.ApplyEffect(player);  // ✅ m_Duration 사용 (일시적 무적)

// ✅ 모두 일관되게 작동! 대체 가능!
```

---

## 📊 코드 구조

### 폴더 구조

```
3_LiskovSubstitution/
├── Scripts/
│   ├── SceneReset.cs                 ← 데모용 (씬 리셋)
│   ├── SpeedModifier.cs              ← 데모용 (속도 수정)
│   │
│   ├── Unrefactored/                 ← ❌ LSP 위반 예시
│   │   ├── UnrefactoredPowerUp.cs    (기본 클래스 - duration 없음)
│   │   └── UnrefactoredSpeedBoost.cs (하위 클래스 - duration 임의 추가)
│   │
│   ├── PowerUps/                     ← ✅ LSP 준수 예시
│   │   ├── PowerUp.cs                (기본 클래스 - m_Duration 포함)
│   │   ├── HealthBoost.cs            (하위 클래스 - 체력 회복)
│   │   ├── InvulnerabilityPowerUp.cs (하위 클래스 - 무적)
│   │   └── SpeedBoost.cs             (하위 클래스 - 속도 증가)
│   │
│   └── Health/                       ← 보조 시스템
│       ├── Health.cs                 (체력 관리)
│       ├── Invulnerability.cs        (무적 기능)
│       └── HealthBar.cs              (체력 UI)
│
└── README.md                          ← 📍 현재 문서
```

### 클래스 다이어그램

```
═══════════════════════════════════════════════════════════════
❌ LSP 위반 (Unrefactored)
═══════════════════════════════════════════════════════════════

                    UnrefactoredPowerUp
                    ┌─────────────────────────┐
                    │ ❌ duration 개념 없음   │
                    ├─────────────────────────┤
                    │ + ApplyEffect(player)   │
                    └─────────────────────────┘
                               △
                               │ extends
                               │
          ┌────────────────────┴────────────────────┐
          │                                         │
┌─────────────────────┐              ┌──────────────────────────┐
│UnrefactoredSpeedBoost│              │ SomeOtherPowerUp         │
├─────────────────────┤              ├──────────────────────────┤
│ ❌ m_Duration = 5f │              │ (duration 개념 없음)     │
│ (임의로 추가!)      │              │                          │
├─────────────────────┤              ├──────────────────────────┤
│ + ApplyEffect()     │              │ + ApplyEffect()          │
│   if (duration>0)   │              │   (즉시 효과)            │
│   else { ??? }      │              │                          │
└─────────────────────┘              └──────────────────────────┘
      ⚠️ 조건부 동작                        ⚠️ 일관성 없음

═══════════════════════════════════════════════════════════════
✅ LSP 준수 (Refactored)
═══════════════════════════════════════════════════════════════

                         PowerUp
        ┌────────────────────────────────────────┐
        │ ✅ m_Duration (protected)              │
        │    모든 하위 클래스가 공유하는 개념   │
        ├────────────────────────────────────────┤
        │ + ApplyEffect(player)                  │
        │ # OnTriggerEnter(other)                │
        │ # PlaySound(player)                    │
        │ # CollectPowerUp()                     │
        └────────────────────────────────────────┘
                         △
                         │ extends
                         │
        ┌────────────────┼────────────────┐
        │                │                │
┌───────────────┐  ┌─────────────┐  ┌─────────────────────┐
│  HealthBoost  │  │ SpeedBoost  │  │InvulnerabilityPowerUp│
├───────────────┤  ├─────────────┤  ├─────────────────────┤
│- m_HealValue  │  │- m_Speed    │  │(m_Duration 상속)    │
│               │  │  Multiplier │  │                     │
│(m_Duration    │  │             │  │                     │
│ 상속받지만    │  │(m_Duration  │  │                     │
│ 사용 안함)    │  │ 사용)       │  │                     │
├───────────────┤  ├─────────────┤  ├─────────────────────┤
│+ ApplyEffect()│  │+ Apply      │  │+ ApplyEffect()      │
│  Heal(50)     │  │  Effect()   │  │  Activate(duration) │
│  (즉시)       │  │  Modify     │  │  (m_Duration 활용)  │
│               │  │  Speed(2f,  │  │                     │
│               │  │  duration)  │  │                     │
└───────────────┘  └─────────────┘  └─────────────────────┘
   ✅ 즉시 효과       ✅ 지속 효과         ✅ 지속 효과
   (duration=0)       (duration>0)         (duration>0)

═══════════════════════════════════════════════════════════════
📝 핵심 차이점
═══════════════════════════════════════════════════════════════

Unrefactored (위반) :
  - duration이 하위 클래스에만 존재
  - 각 하위 클래스가 독자적으로 처리
  - 대체 불가능!

Refactored (준수) :
  - duration이 기본 클래스에 존재
  - 모든 하위 클래스가 공통 개념 공유
  - 사용 여부는 선택적
  - 완벽하게 대체 가능! ✅
```

### 상속 구조 및 데이터 흐름

```
═══════════════════════════════════════════════════════════════
🔄 파워업 실행 흐름 (LSP 준수 버전)
═══════════════════════════════════════════════════════════════

[플레이어가 파워업과 충돌]
         ↓
┌─────────────────────────────────┐
│ PowerUp.OnTriggerEnter(other)  │
│ (기본 클래스의 공통 로직)       │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ 1. 플레이어인지 확인            │
│    if (!other.CompareTag        │
│       ("Player")) return;       │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ 2. 사운드 재생                  │
│    PlaySound(other.gameObject)  │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ 3. 하위 클래스의 효과 적용      │
│    ApplyEffect(other.gameObject)│
│         ↓                       │
│    ┌────┴──────┬─────────┐     │
│    ↓           ↓         ↓     │
│ HealthBoost SpeedBoost Invuln  │
│ Heal(50)   ModifySpeed Activate │
│ (즉시)     (지속)      (지속)   │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ 4. 파워업 수집/제거             │
│    CollectPowerUp()             │
│    → Destroy(gameObject)        │
└─────────────────────────────────┘

═══════════════════════════════════════════════════════════════
```

---

## 🔄 Before & After 비교

### ❌ Before : LSP 미적용 (Unrefactored)

#### 1️⃣ UnrefactoredPowerUp.cs (기본 클래스)

```csharp
/// <summary>
/// 파워업의 기본 클래스 (리팩토링 전)
/// ⚠️ 리스코프 치환 원칙 위반 예시 ⚠️
/// </summary>
public abstract class UnrefactoredPowerUp : MonoBehaviour
{
    const string k_PlayerTag = "Player";

    // ❌ duration 개념 없음!
    public abstract void ApplyEffect(GameObject player);

    protected void OnTriggerEnter(Collider other)
    {
        if (!other.gameObject.CompareTag(k_PlayerTag))
            return;

        ApplyEffect(other.gameObject);
        CollectPowerUp();
    }

    protected void CollectPowerUp()
    {
        Destroy(gameObject);
    }
}
```

**문제점 :**
- 🔴 duration(지속 시간) 개념이 없음
- 🔴 일시적 효과와 즉시 효과를 구분할 방법 없음
- 🔴 하위 클래스가 임의로 추가할 가능성

---

#### 2️⃣ UnrefactoredSpeedBoost.cs (하위 클래스)

```csharp
/// <summary>
/// 리스코프 치환 원칙 위반 예시 클래스
/// ⚠️ LSP 위반 ⚠️
/// </summary>
public class UnrefactoredSpeedBoost : UnrefactoredPowerUp
{
    public float m_SpeedMultiplier = 2f;
    public float m_Duration        = 5f; // ❌ 기본 클래스에 없는 개념!

    public override void ApplyEffect(GameObject player)
    {
        if (m_Duration > 0)  // ❌ 조건부 동작!
        {
            SpeedModifier playerMovement = player.GetComponent<SpeedModifier>();
            if (playerMovement != null)
            {
                playerMovement.ModifySpeed(m_SpeedMultiplier, m_Duration);
            }
        }
        else
        {
            // ❌ duration 없으면? 혼란스러운 동작
        }
    }
}
```

**문제점 :**
- 🔴 기본 클래스에 없는 `m_Duration`을 임의로 추가
- 🔴 `ApplyEffect()`가 조건부로 동작 (일관성 깨짐)
- 🔴 다른 PowerUp과 대체 불가능
- 🔴 기본 클래스의 계약 위반

---

### ✅ After : LSP 적용

#### 1️⃣ PowerUp.cs (기본 클래스 - 리팩토링 완료)

```csharp
/// <summary>
/// 파워업의 기본 클래스 (리팩토링 완료)
/// ✅ 리스코프 치환 원칙 준수 ✅
/// </summary>
[RequireComponent(typeof(Collider))]
public abstract class PowerUp : MonoBehaviour
{
    [Tooltip("파워업 지속 시간 (일시적인 효과인 경우)")]
    [SerializeField] protected float m_Duration;  // ✅ 공통 개념!

    protected const string k_PlayerTag = "Player";

    // 각 하위 클래스에서 오버라이드할 로직
    public abstract void ApplyEffect(GameObject player);

    // 모든 파워업에 공통적인 기능
    protected void OnTriggerEnter(Collider other)
    {
        if (!other.gameObject.CompareTag(k_PlayerTag))
            return;

        PlaySound(other.gameObject);  // 사운드 재생
        ApplyEffect(other.gameObject); // 하위 클래스 로직
        CollectPowerUp();              // 파워업 제거
    }

    protected void PlaySound(GameObject player)
    {
        PlayerAudio m_PlayerAudio = player.GetComponent<PlayerAudio>();

        if (m_PlayerAudio != null)
        {
            m_PlayerAudio.PlayRandomClip();
        }
    }

    protected void CollectPowerUp()
    {
        Destroy(gameObject);
    }
}
```

**개선 사항 :**
- ✅ `m_Duration` 필드를 기본 클래스에 추가
- ✅ 모든 하위 클래스가 duration 개념 공유
- ✅ 일관된 인터페이스 제공
- ✅ 명확한 계약 정의

---

#### 2️⃣ HealthBoost.cs (즉시 효과)

```csharp
/// <summary>
/// 체력 회복 파워업
/// ✅ LSP 준수 ✅
/// </summary>
public class HealthBoost : PowerUp
{
    [Tooltip("회복할 체력량")]
    [SerializeField] private float m_HealValue = 50f;

    public override void ApplyEffect(GameObject player)
    {
        Health playerHealth = player.GetComponent<Health>();

        if (playerHealth != null)
        {
            playerHealth.Heal(m_HealValue);  // ✅ 즉시 회복
            // m_Duration 미사용 (즉시 효과)
        }
    }
}
```

**특징 :**
- ✅ `m_Duration`을 상속받지만 사용하지 않음 (즉시 효과)
- ✅ 기본 클래스의 계약 준수
- ✅ 다른 PowerUp과 대체 가능

---

#### 3️⃣ SpeedBoost.cs (지속 효과)

```csharp
/// <summary>
/// 속도 증가 파워업
/// ✅ LSP 준수 ✅
/// </summary>
public class SpeedBoost : PowerUp
{
    [Header("속도 파라미터")]
    [Tooltip("속도 배율 (2.0이면 2배 빠르게)")]
    [SerializeField] private float m_SpeedMultiplier = 2f;

    public override void ApplyEffect(GameObject player)
    {
        SpeedModifier speedModifier = player.GetComponent<SpeedModifier>();

        if (speedModifier != null)
        {
            // ✅ 기본 클래스의 m_Duration 사용
            speedModifier.ModifySpeed(m_SpeedMultiplier, m_Duration);
        }
    }
}
```

**특징 :**
- ✅ `m_Duration`을 활용 (일시적 효과)
- ✅ 기본 클래스의 계약 준수
- ✅ 다른 PowerUp과 대체 가능

---

#### 4️⃣ InvulnerabilityPowerUp.cs (지속 효과)

```csharp
/// <summary>
/// 무적 파워업
/// ✅ LSP 준수 ✅
/// </summary>
public class InvulnerabilityPowerUp : PowerUp
{
    public override void ApplyEffect(GameObject player)
    {
        Invulnerability invulnerability = player.GetComponent<Invulnerability>();

        if (invulnerability != null)
        {
            // ✅ 기본 클래스의 m_Duration 사용
            invulnerability.Activate(m_Duration);
        }
    }
}
```

**특징 :**
- ✅ `m_Duration`을 활용 (일시적 효과)
- ✅ 기본 클래스의 계약 준수
- ✅ 다른 PowerUp과 대체 가능

---

### 📊 개선 효과

| 항목 | Before (Unrefactored) | After (LSP 적용) |
|------|----------------------|------------------|
| **duration 위치** | 하위 클래스에만 | ✅ 기본 클래스에 |
| **일관성** | ❌ 각각 다름 | ✅ 모두 동일 |
| **대체 가능성** | ❌ 불가능 | ✅ 가능 |
| **계약 명확성** | ❌ 불명확 | ✅ 명확 |
| **확장성** | ❌ 혼란스러움 | ✅ 명확한 규칙 |
| **유지보수** | ❌ 어려움 | ✅ 쉬움 |

---

## 💻 주요 코드 분석

### 📌 핵심 1 : 대체 가능성 테스트

**Before (대체 불가능) :**
```csharp
void CollectAllPowerUps(List<UnrefactoredPowerUp> powerUps)
{
    foreach (var powerUp in powerUps)
    {
        powerUp.ApplyEffect(player);  // ❌ 일관성 없음!

        // UnrefactoredSpeedBoost : duration에 따라 다르게 동작
        // SomeOtherPowerUp : duration 개념 없음
        // → 각각 다르게 동작! 대체 불가능!
    }
}
```

**After (대체 가능) :**
```csharp
void CollectAllPowerUps(List<PowerUp> powerUps)
{
    foreach (var powerUp in powerUps)
    {
        powerUp.ApplyEffect(player);  // ✅ 일관되게 작동!

        // HealthBoost : m_Duration 미사용 (즉시 회복)
        // SpeedBoost : m_Duration 사용 (속도 증가)
        // InvulnerabilityPowerUp : m_Duration 사용 (무적)
        // → 모두 기본 클래스 계약 준수! 대체 가능! ✅
    }
}
```

---

### 📌 핵심 2 : 공통 개념의 상속

**위치 :** PowerUp.cs:17-18

```csharp
[Tooltip("파워업 지속 시간 (일시적인 효과인 경우)")]
[SerializeField] protected float m_Duration;  // ✅ 핵심!
```

**이해 포인트 :**
- `protected` : 하위 클래스에서 접근 가능
- `[SerializeField]` : 인스펙터에서 설정 가능
- **모든 하위 클래스가 상속받음**
- 사용 여부는 각 하위 클래스가 결정 (유연성)

---

### 📌 핵심 3 : 일관된 ApplyEffect() 계약

**위치 :** PowerUp.cs:22

```csharp
public abstract void ApplyEffect(GameObject player);
```

**모든 하위 클래스의 구현 :**

```csharp
// HealthBoost : m_Duration 무시
public override void ApplyEffect(GameObject player)
{
    playerHealth.Heal(m_HealValue);  // 즉시 효과
}

// SpeedBoost : m_Duration 활용
public override void ApplyEffect(GameObject player)
{
    speedModifier.ModifySpeed(m_SpeedMultiplier, m_Duration);  // 지속 효과
}

// InvulnerabilityPowerUp : m_Duration 활용
public override void ApplyEffect(GameObject player)
{
    invulnerability.Activate(m_Duration);  // 지속 효과
}
```

**이해 포인트 :**
- 모든 하위 클래스가 **동일한 시그니처** (파라미터)
- `m_Duration` 사용 여부는 선택적
- 기본 클래스 사용자는 일관된 동작 기대 가능

---

## ⚖️ 장단점

### ✅ 장점

**1. 완벽한 대체 가능성**
- 부모 타입으로 자식 객체 사용 가능
- 다형성(Polymorphism) 올바르게 활용
- 코드 예측 가능성 증가

**2. 일관된 인터페이스**
- 모든 하위 클래스가 같은 계약 준수
- 사용자 입장에서 사용법 명확

**3. 확장성 증가**
- 새로운 파워업 추가 시 규칙 명확
- 기존 코드 수정 불필요

**4. 유지보수 용이**
- 기본 클래스 수정 시 모든 하위 클래스 영향 고려 가능
- 변경 사항 파악 쉬움

**5. 테스트 용이**
- 모든 PowerUp을 동일하게 테스트
- 단위 테스트 작성 쉬움

**6. 설계 명확성**
- 기본 클래스가 공통 개념 정의
- 하위 클래스의 역할 명확

### ❌ 단점

**1. 초기 설계 시간**
- 공통 개념 파악에 시간 필요
- 충분한 분석 필요

**2. 기본 클래스 복잡도**
- 공통 개념이 많아지면 기본 클래스 복잡해짐
- 과도한 일반화 위험

**3. 유연성 제약**
- 모든 하위 클래스가 기본 클래스 규칙 따라야 함
- 완전히 다른 동작 구현 어려움

**4. 리팩토링 비용**
- 기존 코드 LSP 적용 시 대규모 수정 필요
- 테스트 코드도 함께 수정

---

## 🎮 실제 적용 사례

### 1️⃣ 게임 개발

**아이템 시스템**
```csharp
// ✅ LSP 적용
public abstract class Item
{
    protected int stackSize;    // 공통 개념
    public abstract void Use();  // 사용 방법은 각자
}

public class Potion : Item { }       // stackSize 사용
public class Equipment : Item { }    // stackSize = 1 (장비는 1개)
public class Material : Item { }     // stackSize 활용 (재료는 99개)

// 모두 Item으로 대체 가능!
List<Item> inventory = new List<Item>();
```

**적 AI 시스템**
```csharp
// ✅ LSP 적용
public abstract class Enemy
{
    protected float moveSpeed;       // 공통 개념
    protected int   maxHealth;       // 공통 개념
    public abstract void Attack();   // 공격 방식은 각자
}

public class MeleeEnemy : Enemy { }   // 근접 공격
public class RangedEnemy : Enemy { }  // 원거리 공격
public class BossEnemy : Enemy { }    // 특수 공격

// 모든 Enemy를 동일하게 처리!
foreach (Enemy enemy in enemies)
{
    enemy.Attack();  // ✅ 일관되게 작동
}
```

**스킬 시스템**
```csharp
// ✅ LSP 적용
public abstract class Skill
{
    protected float cooldown;        // 공통 개념
    protected float manaCost;        // 공통 개념
    public abstract void Cast();     // 발동 로직은 각자
}

public class FireballSkill : Skill { }
public class HealSkill : Skill { }
public class BuffSkill : Skill { }

// 모든 Skill을 동일하게 관리!
```

### 2️⃣ UI 시스템

```csharp
// ✅ LSP 적용
public abstract class UIPanel
{
    protected bool isVisible;        // 공통 개념
    public abstract void Open();     // 열기
    public abstract void Close();    // 닫기
}

public class MenuPanel : UIPanel { }
public class InventoryPanel : UIPanel { }
public class SettingsPanel : UIPanel { }

// 모든 패널을 동일하게 관리!
```

### 3️⃣ 데이터 저장 시스템

```csharp
// ✅ LSP 적용
public abstract class SaveData
{
    protected string fileName;       // 공통 개념
    public abstract void Save();     // 저장 방식은 각자
    public abstract void Load();     // 로드 방식은 각자
}

public class PlayerSaveData : SaveData { }
public class SettingsSaveData : SaveData { }
public class ProgressSaveData : SaveData { }

// 모든 SaveData를 동일하게 처리!
```

---

## 📝 학습 정리

### 핵심 요약

1. **리스코프 치환 원칙이란?**
   - 부모 타입 변수에 자식 객체를 넣어도 예상대로 작동해야 함
   - "하위 클래스는 상위 클래스를 대체할 수 있어야 한다"

2. **LSP 위반 사례**
   - 하위 클래스가 기본 클래스에 없는 개념을 임의로 추가
   - 조건부 동작으로 일관성 깨짐
   - 대체 불가능한 상속 구조

3. **LSP 준수 방법**
   - 공통 개념을 기본 클래스에 정의
   - 모든 하위 클래스가 동일한 계약 준수
   - 사용 여부는 선택적으로 허용

4. **실전 적용**
   - 기본 클래스에 `m_Duration` 같은 공통 필드 정의
   - 하위 클래스는 필요시 사용, 불필요시 무시
   - 모든 하위 클래스가 일관된 인터페이스 유지

5. **장점**
   - 완벽한 대체 가능성
   - 일관된 인터페이스
   - 확장성 증가
   - 유지보수 용이

6. **주의사항**
   - 충분한 초기 설계 필요
   - 과도한 일반화 주의
   - 기본 클래스 복잡도 관리

### Before vs After

| | Before (Unrefactored) | After (LSP 적용) |
|---|----------------------|------------------|
| **duration 위치** | 하위 클래스 | ✅ 기본 클래스 |
| **일관성** | ❌ 각각 다름 | ✅ 모두 동일 |
| **대체 가능** | ❌ 불가능 | ✅ 가능 |
| **계약** | ❌ 불명확 | ✅ 명확 |
| **확장** | ❌ 혼란 | ✅ 명확 |

### 실무 적용 팁

✅ **이런 경우 LSP 점검 필요**
- 하위 클래스가 기본 클래스 메서드를 빈 상태로 구현
- 하위 클래스에서 예외를 던져야 하는 경우
- 하위 클래스가 조건부로 동작하는 경우
- 타입 체크(`is`, `as`)를 자주 사용하는 경우

❌ **LSP 위반 징후**
```csharp
// ❌ 타입 체크 필요 = LSP 위반 가능성
if (powerUp is UnrefactoredSpeedBoost)
{
    // 특별 처리
}
```

✅ **LSP 준수 코드**
```csharp
// ✅ 타입 체크 불필요 = LSP 준수
powerUp.ApplyEffect(player);  // 모두 일관되게 작동
```