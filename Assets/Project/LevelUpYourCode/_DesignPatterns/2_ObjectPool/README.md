# 🎯 Object Pool Pattern - 오브젝트 풀 패턴

## 📋 목차
- [개요](#-개요)
- [오브젝트 풀링이란?](#-오브젝트-풀링이란)
- [파일 구조](#-파일-구조)
- [핵심 코드 분석](#-핵심-코드-분석)
- [사용 흐름](#-사용-흐름)
- [Unity ObjectPool API](#-unity-objectpool-api)
- [장단점](#-장단점)

---

## 🎯 개요

**학습 완료일 :** 2026.01.22

**핵심 개념 :** `Instantiate()` / `Destroy()` 대신 오브젝트를 **재사용**하여 성능을 최적화하는 패턴

**사용 사례 :**
- 총알, 파티클 등 빈번하게 생성/파괴되는 오브젝트
- 적 캐릭터, 아이템 스폰
- UI 요소 동적 생성

---

## 🔍 오브젝트 풀링이란?

### ❌ 기존 방식의 문제점

```csharp
// 발사할 때마다 새로 생성
GameObject bullet = Instantiate(bulletPrefab);

// 사용 후 파괴
Destroy(bullet);
```

🔸 매번 **메모리 할당/해제** 발생

🔸 **가비지 컬렉션(GC)** 부하 → 프레임 드랍

🔸 모바일에서 특히 심각한 성능 저하

### ✅ 오브젝트 풀링 방식

```csharp
// 풀에서 가져오기 (이미 생성된 오브젝트 재사용)
GameObject bullet = pool.Get();

// 풀에 반환 (파괴하지 않고 비활성화)
pool.Release(bullet);
```

🔸 **미리 생성**해둔 오브젝트를 재사용

🔸 GC 부하 **최소화**

🔸 **일관된 성능** 유지

---

## 📁 파일 구조

```
2_ObjectPool/
├── Scripts/
│   ├── ExampleUsage/                    ← ⭐ Unity API 사용 (권장)
│   │   ├── RevisedGun.cs                # 총기 (풀 관리 + 클라이언트)
│   │   └── RevisedProjectile.cs         # 발사체
│   │
│   └── Pattern/ManualExample/           ← 직접 구현 (학습용)
│       ├── ObjectPool.cs                # 풀 관리자
│       ├── PooledObject.cs              # 풀링 오브젝트 기본 클래스
│       ├── ExampleGun.cs                # 총기 (클라이언트)
│       └── ExampleProjectile.cs         # 발사체
│
└── README.md                            # 📍 현재 문서
```


> 💡 **실제 프로젝트에서는 Unity API 사용을 권장합니다.**

---

## 🔧 핵심 코드 분석

### 1. RevisedGun.cs - 총기 (풀 관리자 + 클라이언트)

Unity의 `ObjectPool<T>`를 사용하여 발사체를 관리합니다.

```csharp
using UnityEngine.Pool;

public class RevisedGun : MonoBehaviour
{
    [SerializeField] private RevisedProjectile projectilePrefab;

    // Unity 2021+ 에서 제공하는 스택 기반 ObjectPool
    private IObjectPool<RevisedProjectile> objectPool;

    [SerializeField] private bool collectionCheck = true;  // 중복 반환 체크
    [SerializeField] private int  defaultCapacity = 20;    // 초기 용량
    [SerializeField] private int  maxSize         = 100;   // 최대 크기
```

#### 풀 초기화 - 4개의 콜백 등록

```csharp
private void Awake()
{
    objectPool = new ObjectPool<RevisedProjectile>(
        CreateProjectile,        // 생성 콜백
        OnGetFromPool,           // 풀에서 꺼낼 때 콜백
        OnReleaseToPool,         // 풀에 반환할 때 콜백
        OnDestroyPooledObject,   // 오브젝트 파괴 콜백
        collectionCheck,         // 중복 반환 체크
        defaultCapacity,         // 초기 용량
        maxSize                  // 최대 크기
    );
}
```

#### 4개의 콜백 함수

```csharp
// 1. 생성 콜백 : 풀에 오브젝트가 부족할 때 호출
private RevisedProjectile CreateProjectile()
{
    RevisedProjectile instance = Instantiate(projectilePrefab);
    instance.ObjectPool        = objectPool; // 풀 참조 설정
    return instance;
}

// 2. 꺼낼 때 콜백 : pool.Get() 호출 시
private void OnGetFromPool(RevisedProjectile pooledObject)
{
    pooledObject.gameObject.SetActive(true);
}

// 3. 반환할 때 콜백 : pool.Release() 호출 시
private void OnReleaseToPool(RevisedProjectile pooledObject)
{
    pooledObject.gameObject.SetActive(false);
}

// 4. 파괴 콜백 : maxSize 초과 시
private void OnDestroyPooledObject(RevisedProjectile pooledObject)
{
    Destroy(pooledObject.gameObject);
}
```

#### 발사 로직

```csharp
private void Shoot()
{
    // Instantiate 대신 풀에서 가져옴
    RevisedProjectile bullet = objectPool.Get();

    // 위치 설정 및 발사
    bullet.transform.SetPositionAndRotation(muzzlePosition.position, muzzlePosition.rotation);
    bullet.GetComponent<Rigidbody>().AddForce(bullet.transform.forward * muzzleVelocity, ForceMode.Acceleration);

    // 일정 시간 후 풀에 반환
    bullet.Deactivate();
}
```

---

### 2. RevisedProjectile.cs - 발사체

자신이 속한 풀에 대한 참조를 가지고, 일정 시간 후 스스로 반환합니다.

```csharp
using UnityEngine.Pool;

public class RevisedProjectile : MonoBehaviour
{
    [SerializeField] private float timeoutDelay = 3f;

    // 풀 참조 (RevisedGun에서 설정)
    private IObjectPool<RevisedProjectile> objectPool;
    public IObjectPool<RevisedProjectile> ObjectPool { set => objectPool = value; }

    public void Deactivate()
    {
        StartCoroutine(DeactivateRoutine(timeoutDelay));
    }

    private IEnumerator DeactivateRoutine(float delay)
    {
        yield return new WaitForSeconds(delay);

        // Rigidbody 속도 초기화
        Rigidbody rBody       = GetComponent<Rigidbody>();
        rBody.linearVelocity  = Vector3.zero;
        rBody.angularVelocity = Vector3.zero;

        // 풀에 반환 (Destroy 대신!)
        objectPool.Release(this);
    }
}
```

---

## 🔄 사용 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                         게임 시작                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ObjectPool 생성 (defaultCapacity 만큼 미리 생성하지 않음)      │
│  → Unity의 ObjectPool은 Lazy 방식 (필요할 때 생성)              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    🔫 발사 (Fire1 입력)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌───────────────────┐                     ┌───────────────────┐
│ 풀이 비어있음     │                     │ 풀에 오브젝트 있음 │
│                   │                     │                   │
│ CreateProjectile()│                     │ stack.Pop()       │
│ 새로 생성         │                     │ 기존 오브젝트 재사용│
└───────────────────┘                     └───────────────────┘
        │                                           │
        └─────────────────────┬─────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  OnGetFromPool() 콜백 → SetActive(true)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  발사체 이동 (3초 동안)                                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  objectPool.Release(this) 호출                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌───────────────────┐                     ┌───────────────────┐
│ 풀 크기 < maxSize │                     │ 풀 크기 >= maxSize│
│                   │                     │                   │
│ OnReleaseToPool() │                     │ OnDestroyPooled() │
│ stack.Push()      │                     │ Destroy()         │
│ 풀에 반환         │                     │ 오브젝트 파괴     │
└───────────────────┘                     └───────────────────┘
```

---

## 📖 Unity ObjectPool API

### ObjectPool<T> 생성자

```csharp
public ObjectPool<T>(
    Func<T> createFunc,                    // 생성 콜백 (필수)
    Action<T> actionOnGet = null,          // Get 시 콜백
    Action<T> actionOnRelease = null,      // Release 시 콜백
    Action<T> actionOnDestroy = null,      // 파괴 시 콜백
    bool collectionCheck = true,           // 중복 반환 체크
    int defaultCapacity = 10,              // 초기 용량
    int maxSize = 10000                    // 최대 크기
);
```

### 주요 메서드

| 메서드 | 설명 |
|--------|------|
| `T Get()` | 풀에서 오브젝트를 가져옴 (없으면 생성) |
| `void Release(T element)` | 오브젝트를 풀에 반환 |
| `void Clear()` | 풀의 모든 오브젝트를 파괴 |
| `int CountInactive` | 풀에 있는 비활성 오브젝트 수 |

### collectionCheck 옵션

```csharp
// collectionCheck = true 일 때
pool.Release(bullet);
pool.Release(bullet); // ⚠️ InvalidOperationException 발생!

// collectionCheck = false 일 때
pool.Release(bullet);
pool.Release(bullet); // 중복 반환되어 버그 발생 가능
```

> 💡 **개발 중에는 `true`로 설정**하여 버그를 조기에 발견하고, **릴리즈 빌드에서는 `false`**로 성능 최적화

---

## ⚖️ 장단점

### ✅ 장점

🔸 **성능 향상** : GC 부하 감소, 일관된 프레임 레이트

🔸 **메모리 효율** : 최대 크기 제한으로 메모리 사용량 제어

🔸 **간편한 관리** : Unity API의 콜백 시스템으로 라이프사이클 관리

🔸 **안전성** : collectionCheck로 중복 반환 방지

### ❌ 단점

🔸 **초기 설정 필요** : 적절한 풀 크기 예측 필요

🔸 **상태 초기화** : 재사용 시 오브젝트 상태 리셋 필요 (속도, 위치 등)

🔸 **복잡도 증가** : 단순한 경우 오히려 오버엔지니어링

---

## 💡 실무 팁

### 1. 적절한 풀 크기 설정

```csharp
// 동시에 화면에 보일 수 있는 최대 수 + 여유분
[SerializeField] private int defaultCapacity = 20;
[SerializeField] private int maxSize         = 100;
```

### 2. 상태 초기화 잊지 않기

```csharp
private void OnGetFromPool(RevisedProjectile obj)
{
    obj.gameObject.SetActive(true);

    // 상태 초기화
    obj.ResetState();
}
```

### 3. 씬 전환 시 풀 정리

```csharp
private void OnDestroy()
{
    objectPool.Clear();
}
```

---

**📅 마지막 업데이트 :** 2026.01.22
